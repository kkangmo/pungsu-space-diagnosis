# 복사 후 실제 키로 교체하세요. 이 파일은 커밋해도 안전합니다.
# 실제 키는 .env 에만 ( .gitignore 에 .env 포함됨 )

# 지오코딩 (주소→좌표). 없으면 Nominatim(OSM) 폴백으로 동작
KAKAO_REST_KEY=

# 지도 타일. 없으면 Leaflet + OpenStreetMap 폴백으로 동작
KAKAO_JS_KEY=

# 사진 분석 (Gemini Flash free tier). 없으면 사진 분석 안내 문구로 대체
GEMINI_API_KEY=
